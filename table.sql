CREATE TABLE STUDENT(
    STU_ID CHAR(5),PRIMARY KEY,
    stu name CHAR(10),
    dob DATE,
    address varchar(100),
    mobile int

);

CREATE table attendence(
    attem_id char(3),
    name CHAR(100)
);